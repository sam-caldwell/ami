// file: main.ami
package main

// Capabilities required by io.* use at egress.
#pragma capabilities list=io

// Single pipeline: ingress fires every 1s; egress writes the time to stdout.
// Grammar per spec: node calls with '()'; edge via '.'; attributes follow the node.
pipeline Clock(){
  ingress() schedule(1s).egress() io.Stdout;
}
