// file: main.ami
package main

// Declare capabilities required by io.* at egress (per docx and semantics).
#pragma capabilities list=io

// Single-pipeline demo: Ingress fires a 1s interval timer; Egress writes time to stdout.
// Grammar: nodes are called with '()' and edges are expressed by '.' chaining.
pipeline Clock(){
  Ingress().Timer(1s).io.Stdout().Egress();
}
