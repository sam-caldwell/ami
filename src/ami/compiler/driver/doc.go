// Package driver exposes the public compiler entry point.
// It compiles source files into IR, performs basic semantic checks,
// and (optionally) emits debug artifacts under build/debug.
package driver

