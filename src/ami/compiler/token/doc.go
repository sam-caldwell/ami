// Package token defines lexical token kinds, operator precedences,
// keyword lookup, and a minimal token representation used by the
// scanner and parser. This package is deliberately declarative and
// self-contained to avoid cross-package churn.
package token
