//go:build cuda && linux && amd64

package gpu

// Placeholder for CUDA linux/amd64 implementation. When enabled with the
// 'cuda' build tag, real CUDA bindings should be provided here. Stub
// implementations live in gpu.go and are compiled when this tag is not set.
const _cudaLinuxAMD64Placeholder = true
