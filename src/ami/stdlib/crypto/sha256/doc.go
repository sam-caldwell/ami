// Package sha256 provides deterministic SHA-256 hashing helpers for AMI stdlib.
//
// Phase 1: scaffold only (layout and conventions). Implementation to follow
// per SPECIFICATION.md §11 (Encoding/Hashing).
package sha256
