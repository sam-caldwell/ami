// Package os provides restricted filesystem APIs for AMI stdlib
// (explicit permissions, no env/process mutation).
//
// Phase 1: scaffold only (layout and conventions). Implementation to follow
// per SPECIFICATION.md §11 (Filesystem/I/O).
package os
