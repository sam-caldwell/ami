// Package filepath provides deterministic path manipulation primitives for AMI stdlib.
// No symlink evaluation; pure string transformations only.
//
// Phase 1: scaffold only (layout and conventions). Implementation to follow
// per SPECIFICATION.md §11 (Filesystem/I/O).
package filepath
