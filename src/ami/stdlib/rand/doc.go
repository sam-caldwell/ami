// Package rand provides deterministic PRNG interfaces for AMI stdlib.
// No package-level global RNGs; explicit seed handles only.
//
// Phase 1: scaffold only (layout and conventions). Implementation to follow
// per SPECIFICATION.md §11 (Time/Math).
package rand
