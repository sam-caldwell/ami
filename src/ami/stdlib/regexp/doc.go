// Package regexp provides a restricted, deterministic subset of regexp APIs
// suitable for AMI stdlib use.
//
// Phase 1: scaffold only (layout and conventions). Implementation to follow
// per SPECIFICATION.md §11 (Core Text/Data).
package regexp
