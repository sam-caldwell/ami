// Package time provides deterministic time handling for AMI stdlib
// (e.g., formatting/parsing via injected clock interfaces only).
//
// Phase 1: scaffold only (layout and conventions). Implementation to follow
// per SPECIFICATION.md §11 (Time/Math).
package time
